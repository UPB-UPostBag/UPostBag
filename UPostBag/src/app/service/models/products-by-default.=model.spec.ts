import { ProductsByDefault } from './products-by-default.=model';

describe('ProductsByDefault', () => {
  it('should create an instance', () => {
    expect(new ProductsByDefault()).toBeTruthy();
  });
});
